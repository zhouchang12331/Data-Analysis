# 五、思考题与课后作业（第 1、2 题）

## 1. 代数证明题：一元线性回归残差的两个性质

**题目**：在一元线性回归中证明残差之和必然等于零（$\sum_{i=1}^n e_i = 0$），且残差与自变量乘积之和也必然等于零（$\sum_{i=1}^n x_i e_i = 0$）。

### 证明（基于最小二乘的一阶条件）

设一元线性回归模型为

$$
y_i = \beta_0 + \beta_1 x_i + e_i, \qquad i = 1, 2, \dots, n
$$

其中 $e_i = y_i - \hat\beta_0 - \hat\beta_1 x_i$ 为残差。最小二乘法通过最小化残差平方和

$$
RSS(\beta_0, \beta_1) = \sum_{i=1}^{n} \left( y_i - \beta_0 - \beta_1 x_i \right)^2
$$

来估计参数 $\hat\beta_0, \hat\beta_1$。由于 $RSS$ 是 $(\beta_0, \beta_1)$ 的二次可微函数，极小值点必须满足两个一阶条件（对参数求偏导并令其为零）：

**第一步：对截距 $\beta_0$ 求偏导**

$$
\frac{\partial RSS}{\partial \beta_0}
= -2 \sum_{i=1}^{n} \left( y_i - \beta_0 - \beta_1 x_i \right)
= 0
$$

将估计值 $\hat\beta_0, \hat\beta_1$ 代入，并记 $e_i = y_i - \hat\beta_0 - \hat\beta_1 x_i$，得

$$
\sum_{i=1}^{n} \left( y_i - \hat\beta_0 - \hat\beta_1 x_i \right) = 0
\quad\Longrightarrow\quad
\boxed{\sum_{i=1}^{n} e_i = 0}
$$

**第二步：对斜率 $\beta_1$ 求偏导**

$$
\frac{\partial RSS}{\partial \beta_1}
= -2 \sum_{i=1}^{n} x_i \left( y_i - \beta_0 - \beta_1 x_i \right)
= 0
$$

同样代入估计值并利用残差定义，得

$$
\sum_{i=1}^{n} x_i \left( y_i - \hat\beta_0 - \hat\beta_1 x_i \right) = 0
\quad\Longrightarrow\quad
\boxed{\sum_{i=1}^{n} x_i e_i = 0}
$$

**证明完毕。** ∎

### 附：矩阵形式（统一视角）

若把模型写成矩阵形式 $\mathbf{y} = \mathbf{X}\boldsymbol{\beta} + \mathbf{e}$，其中 $\mathbf{X}$ 的第一列全为 1（对应截距），第二列为 $\mathbf{x}$，则最小二乘正规方程为

$$
\mathbf{X}^{\top} \mathbf{e} = \mathbf{0}
$$

展开后：

- 第 1 行：$\sum_{i=1}^{n} 1 \cdot e_i = 0$，即 $\sum e_i = 0$；
- 第 2 行：$\sum_{i=1}^{n} x_i e_i = 0$。

这与上述逐项求导的结果完全一致，说明这两个性质是最小二乘“残差与设计矩阵列正交”的直接推论。

### 直观含义

- $\sum e_i = 0$：拟合直线必然穿过数据的均值点 $(\bar x, \bar y)$，残差在正负方向上相互抵消，平均残差恰为 0；
- $\sum x_i e_i = 0$：残差与自变量不相关（样本内正交），说明 $x$ 中可解释的信息已被 $\hat\beta_1$ 提取干净，残差中不再残留与 $x$ 线性相关的成分。

---

## 2. 理解思考题：加入无关特征后 $R^2$ 与 $R^2_{adj}$ 为何方向相反？

**题目**：为什么在多元回归中，往模型里添加一个纯随机生成的无关特征，$R^2$ 会上升或保持不变，而 $R^2_{adj}$ 会下降？请用自由度与 RSS 变动解释。

### 关键公式

设样本量为 $n$，模型含 $p$ 个解释变量（不含截距），记

$$
R^2 = 1 - \frac{RSS}{TSS}, \qquad
R^2_{adj} = 1 - \frac{RSS/(n-p-1)}{TSS/(n-1)}
= 1 - (1 - R^2)\frac{n-1}{n-p-1}
$$

其中 $RSS = \sum e_i^2$ 是残差平方和，$TSS = \sum (y_i - \bar y)^2$ 只由 $y$ 决定、与模型无关。

### 1. 为什么 $R^2$ 只升不降

把新变量 $z$ 加入模型后，最小二乘是在一个更大的参数空间里寻找最优解。而原模型是“新模型取 $z$ 的系数为 0”的一个特例，因此：

$$
RSS_{\text{new}} = \min_{\beta, \gamma} \sum \left(y_i - \beta^{\top}\mathbf{x}_i - \gamma z_i\right)^2
\;\le\;
\underbrace{\sum \left(y_i - \hat\beta^{\top}\mathbf{x}_i - 0 \cdot z_i\right)^2}_{RSS_{\text{old}}}
$$

即新模型的 RSS 不会比旧模型更大（若 $z$ 与现有变量完全共线，最优解与原模型相同，RSS 不变，$R^2$ 保持不变；否则因额外拟合了 $z$ 中与 $y$ 的样本内偶然相关，RSS 严格下降）。由于 $TSS$ 不变，所以

$$
R^2 = 1 - \frac{RSS}{TSS} \quad \text{单调不降，通常严格上升。}
$$

注意：这里的上升很大程度是**样本内过拟合**——纯随机变量 $z$ 与 $y$ 没有真实关系，但有限样本下两者总存在偶然的相关性，最小二乘会把这份“噪声相关性”也当作信号拟合进去。

### 2. 为什么 $R^2_{adj}$ 会下降

$R^2_{adj}$ 对模型复杂度做了**惩罚**。由公式

$$
R^2_{adj} = 1 - (1 - R^2)\frac{n-1}{n-p-1}
$$

加入一个新变量后 $p \to p+1$，自由度 $n-p-1$ 减小，惩罚系数 $\dfrac{n-1}{n-p-1}$ 增大。因此 $R^2_{adj}$ 的变动取决于两股力量的对比：

- **分子力量（收益）**：$RSS$ 下降使 $1-R^2 = RSS/TSS$ 减小；
- **分母力量（惩罚）**：自由度 $n-p-1$ 减小使 $1/(n-p-1)$ 增大。

对于**纯随机无关特征**，它在样本内解释的只是噪声，其对 RSS 的期望下降量约为 $MSE_{\text{old}}$（旧模型的残差方差）量级，远不足以抵消一个自由度被消耗的惩罚。用增量 F 检验的视角看：加入 $z$ 后 $R^2_{adj}$ 上升的充要条件是 $z$ 的 t 统计量满足 $|t| > 1$；而纯随机噪声的 $|t|$ 期望上远小于 1，故

$$
\boxed{R^2 \text{ 上升（或不变），而 } R^2_{adj} \text{ 下降}}
$$

### 3. 结论小结

| 指标 | 加入无关特征后的变化 | 原因 |
|---|---|---|
| $R^2$ | 上升或保持不变 | 新模型是旧模型的特例（$z$ 系数可取 0），最小二乘最优解只可能使 $RSS$ 不增；$TSS$ 不变，故 $R^2$ 不降 |
| $R^2_{adj}$ | 下降 | 自由度 $n-p-1$ 被消耗，惩罚项 $\frac{n-1}{n-p-1}$ 增大；随机噪声带来的 $RSS$ 下降不足以补偿自由度损失 |

**启示**：$R^2$ 只衡量“拟合得好不好看”，会随变量增多单调不降，不能用来比较模型；$R^2_{adj}$ 在拟合优度与模型简洁度之间做了权衡，能惩罚无效变量，更适合用于模型比较与变量筛选。

> 附注：$R^2_{adj}$ 不升反降的临界条件是新增变量的 $|t| < 1$（等价于其样本内解释力低于随机噪声水平），这与增量 F 检验、AIC/BIC 的变量取舍思想是一致的。
